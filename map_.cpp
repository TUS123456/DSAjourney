#include<bits/stdc++.h>
using namespace std;
main(){
	unordered_map<int,pair<int,bool>> visited;
	visited[0]={23,true};	
	for(auto it : visited){
		for(auto pt : it.second{
			cout<<pt.first<<" "<<pt.second;
		}
	}
}